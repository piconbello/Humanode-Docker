services:
  humanode:
    build: .
    image: hmnd-validator:latest
    container_name: humanode-validator
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "30333:30333/tcp"
    stop_grace_period: 35s
